```py
import lib
var year
out('Enter a year: ')
in(year)
if(year%4 == 0)
    if(year%100 == 0)
        if(year%400 == 0)
            out(year + ' is a leap year.')
        else
            out(year + ' is not a leap year.')
    else
        out(year + ' is a leap year.')
else
    out(year + ' is not a leap year.')
```
